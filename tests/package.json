{
  "name": "aws-cms-serverless",
  "version": "1.0.0",
  "description": "This project implements a serverless backend for a simple cms management.",
  "main": "index.js",
  "scripts": {
    "test": "concurrently \"NODE_ENV=test serverless offline start\" \"wait-on http://localhost:4000/dev && NODE_ENV=test sequelize db:migrate:undo:all && NODE_ENV=test sequelize db:migrate && NODE_ENV=test sequelize db:seed:all && NODE_ENV=test jest && NODE_ENV=test sequelize db:migrate:undo:all\"",
    "dev": "concurrently \"NODE_ENV=dev serverless offline start\" \"wait-on http://localhost:4000/dev && NODE_ENV=dev sequelize db:create && NODE_ENV=dev sequelize db:migrate:undo:all && NODE_ENV=dev sequelize db:migrate && NODE_ENV=dev sequelize db:seed:all\""
  },
  "repository": "git+https://github.com/andreciornavei/aws-cms-serverless.git",
  "keywords": [
    "Aws",
    "Serverless",
    "Terraform",
    "Cms",
    "Nodejs"
  ],
  "author": "André Ciornavei <andre.ciornavei@gmail.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/andreciornavei/aws-cms-serverless/issues"
  },
  "homepage": "https://github.com/andreciornavei/aws-cms-serverless#readme",
  "devDependencies": {
    "concurrently": "^5.2.0",
    "jest": "^26.0.1",
    "sqlite3": "^4.2.0",
    "supertest": "^4.0.2",
    "wait-on": "^5.0.0"
  }
}
